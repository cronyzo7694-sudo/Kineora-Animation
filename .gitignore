# toolchains & system junk (home-dir artifacts)
.cargo/
.rustup/
.npm/
.profile
.cache/
.git-credentials

# deps & build artifacts
node_modules/
target/
dist/
*.tmp
*.tsbuildinfo

# generated WASM package (rebuilt by `npm run wasm`)
animator/ui/public/wasm/
# legacy/stale wasm output location (BUG: wasm-pack resolved --out-dir
# relative to the crate dir; build-wasm.sh removes + we never commit it)
animator/core/public/

# OS
.DS_Store
Thumbs.db

# editor (keep .vscode config committed — protocol §23)
.idea/
